// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/denkhaus/agentforge/internal/database/ent/fork"
	"github.com/denkhaus/agentforge/internal/database/ent/predicate"
	"github.com/denkhaus/agentforge/internal/database/ent/repository"
)

// ForkQuery is the builder for querying Fork entities.
type ForkQuery struct {
	config
	ctx              *QueryContext
	order            []fork.OrderOption
	inters           []Interceptor
	predicates       []predicate.Fork
	withOriginalRepo *RepositoryQuery
	withFKs          bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ForkQuery builder.
func (fq *ForkQuery) Where(ps ...predicate.Fork) *ForkQuery {
	fq.predicates = append(fq.predicates, ps...)
	return fq
}

// Limit the number of records to be returned by this query.
func (fq *ForkQuery) Limit(limit int) *ForkQuery {
	fq.ctx.Limit = &limit
	return fq
}

// Offset to start from.
func (fq *ForkQuery) Offset(offset int) *ForkQuery {
	fq.ctx.Offset = &offset
	return fq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fq *ForkQuery) Unique(unique bool) *ForkQuery {
	fq.ctx.Unique = &unique
	return fq
}

// Order specifies how the records should be ordered.
func (fq *ForkQuery) Order(o ...fork.OrderOption) *ForkQuery {
	fq.order = append(fq.order, o...)
	return fq
}

// QueryOriginalRepo chains the current query on the "original_repo" edge.
func (fq *ForkQuery) QueryOriginalRepo() *RepositoryQuery {
	query := (&RepositoryClient{config: fq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(fork.Table, fork.FieldID, selector),
			sqlgraph.To(repository.Table, repository.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, fork.OriginalRepoTable, fork.OriginalRepoColumn),
		)
		fromU = sqlgraph.SetNeighbors(fq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Fork entity from the query.
// Returns a *NotFoundError when no Fork was found.
func (fq *ForkQuery) First(ctx context.Context) (*Fork, error) {
	nodes, err := fq.Limit(1).All(setContextOp(ctx, fq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{fork.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fq *ForkQuery) FirstX(ctx context.Context) *Fork {
	node, err := fq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Fork ID from the query.
// Returns a *NotFoundError when no Fork ID was found.
func (fq *ForkQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = fq.Limit(1).IDs(setContextOp(ctx, fq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{fork.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fq *ForkQuery) FirstIDX(ctx context.Context) string {
	id, err := fq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Fork entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Fork entity is found.
// Returns a *NotFoundError when no Fork entities are found.
func (fq *ForkQuery) Only(ctx context.Context) (*Fork, error) {
	nodes, err := fq.Limit(2).All(setContextOp(ctx, fq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{fork.Label}
	default:
		return nil, &NotSingularError{fork.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fq *ForkQuery) OnlyX(ctx context.Context) *Fork {
	node, err := fq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Fork ID in the query.
// Returns a *NotSingularError when more than one Fork ID is found.
// Returns a *NotFoundError when no entities are found.
func (fq *ForkQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = fq.Limit(2).IDs(setContextOp(ctx, fq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{fork.Label}
	default:
		err = &NotSingularError{fork.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fq *ForkQuery) OnlyIDX(ctx context.Context) string {
	id, err := fq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Forks.
func (fq *ForkQuery) All(ctx context.Context) ([]*Fork, error) {
	ctx = setContextOp(ctx, fq.ctx, ent.OpQueryAll)
	if err := fq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Fork, *ForkQuery]()
	return withInterceptors[[]*Fork](ctx, fq, qr, fq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fq *ForkQuery) AllX(ctx context.Context) []*Fork {
	nodes, err := fq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Fork IDs.
func (fq *ForkQuery) IDs(ctx context.Context) (ids []string, err error) {
	if fq.ctx.Unique == nil && fq.path != nil {
		fq.Unique(true)
	}
	ctx = setContextOp(ctx, fq.ctx, ent.OpQueryIDs)
	if err = fq.Select(fork.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fq *ForkQuery) IDsX(ctx context.Context) []string {
	ids, err := fq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fq *ForkQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fq.ctx, ent.OpQueryCount)
	if err := fq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fq, querierCount[*ForkQuery](), fq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fq *ForkQuery) CountX(ctx context.Context) int {
	count, err := fq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fq *ForkQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fq.ctx, ent.OpQueryExist)
	switch _, err := fq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fq *ForkQuery) ExistX(ctx context.Context) bool {
	exist, err := fq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ForkQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fq *ForkQuery) Clone() *ForkQuery {
	if fq == nil {
		return nil
	}
	return &ForkQuery{
		config:           fq.config,
		ctx:              fq.ctx.Clone(),
		order:            append([]fork.OrderOption{}, fq.order...),
		inters:           append([]Interceptor{}, fq.inters...),
		predicates:       append([]predicate.Fork{}, fq.predicates...),
		withOriginalRepo: fq.withOriginalRepo.Clone(),
		// clone intermediate query.
		sql:  fq.sql.Clone(),
		path: fq.path,
	}
}

// WithOriginalRepo tells the query-builder to eager-load the nodes that are connected to
// the "original_repo" edge. The optional arguments are used to configure the query builder of the edge.
func (fq *ForkQuery) WithOriginalRepo(opts ...func(*RepositoryQuery)) *ForkQuery {
	query := (&RepositoryClient{config: fq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fq.withOriginalRepo = query
	return fq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ForkURL string `json:"fork_url,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Fork.Query().
//		GroupBy(fork.FieldForkURL).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (fq *ForkQuery) GroupBy(field string, fields ...string) *ForkGroupBy {
	fq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ForkGroupBy{build: fq}
	grbuild.flds = &fq.ctx.Fields
	grbuild.label = fork.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ForkURL string `json:"fork_url,omitempty"`
//	}
//
//	client.Fork.Query().
//		Select(fork.FieldForkURL).
//		Scan(ctx, &v)
func (fq *ForkQuery) Select(fields ...string) *ForkSelect {
	fq.ctx.Fields = append(fq.ctx.Fields, fields...)
	sbuild := &ForkSelect{ForkQuery: fq}
	sbuild.label = fork.Label
	sbuild.flds, sbuild.scan = &fq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ForkSelect configured with the given aggregations.
func (fq *ForkQuery) Aggregate(fns ...AggregateFunc) *ForkSelect {
	return fq.Select().Aggregate(fns...)
}

func (fq *ForkQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fq); err != nil {
				return err
			}
		}
	}
	for _, f := range fq.ctx.Fields {
		if !fork.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fq.path != nil {
		prev, err := fq.path(ctx)
		if err != nil {
			return err
		}
		fq.sql = prev
	}
	return nil
}

func (fq *ForkQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Fork, error) {
	var (
		nodes       = []*Fork{}
		withFKs     = fq.withFKs
		_spec       = fq.querySpec()
		loadedTypes = [1]bool{
			fq.withOriginalRepo != nil,
		}
	)
	if fq.withOriginalRepo != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, fork.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Fork).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Fork{config: fq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := fq.withOriginalRepo; query != nil {
		if err := fq.loadOriginalRepo(ctx, query, nodes, nil,
			func(n *Fork, e *Repository) { n.Edges.OriginalRepo = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (fq *ForkQuery) loadOriginalRepo(ctx context.Context, query *RepositoryQuery, nodes []*Fork, init func(*Fork), assign func(*Fork, *Repository)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*Fork)
	for i := range nodes {
		if nodes[i].repository_forks == nil {
			continue
		}
		fk := *nodes[i].repository_forks
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(repository.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "repository_forks" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (fq *ForkQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fq.querySpec()
	_spec.Node.Columns = fq.ctx.Fields
	if len(fq.ctx.Fields) > 0 {
		_spec.Unique = fq.ctx.Unique != nil && *fq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fq.driver, _spec)
}

func (fq *ForkQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(fork.Table, fork.Columns, sqlgraph.NewFieldSpec(fork.FieldID, field.TypeString))
	_spec.From = fq.sql
	if unique := fq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fq.path != nil {
		_spec.Unique = true
	}
	if fields := fq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, fork.FieldID)
		for i := range fields {
			if fields[i] != fork.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fq *ForkQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fq.driver.Dialect())
	t1 := builder.Table(fork.Table)
	columns := fq.ctx.Fields
	if len(columns) == 0 {
		columns = fork.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fq.sql != nil {
		selector = fq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fq.ctx.Unique != nil && *fq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fq.predicates {
		p(selector)
	}
	for _, p := range fq.order {
		p(selector)
	}
	if offset := fq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForkGroupBy is the group-by builder for Fork entities.
type ForkGroupBy struct {
	selector
	build *ForkQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fgb *ForkGroupBy) Aggregate(fns ...AggregateFunc) *ForkGroupBy {
	fgb.fns = append(fgb.fns, fns...)
	return fgb
}

// Scan applies the selector query and scans the result into the given value.
func (fgb *ForkGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fgb.build.ctx, ent.OpQueryGroupBy)
	if err := fgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ForkQuery, *ForkGroupBy](ctx, fgb.build, fgb, fgb.build.inters, v)
}

func (fgb *ForkGroupBy) sqlScan(ctx context.Context, root *ForkQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fgb.fns))
	for _, fn := range fgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fgb.flds)+len(fgb.fns))
		for _, f := range *fgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ForkSelect is the builder for selecting fields of Fork entities.
type ForkSelect struct {
	*ForkQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fs *ForkSelect) Aggregate(fns ...AggregateFunc) *ForkSelect {
	fs.fns = append(fs.fns, fns...)
	return fs
}

// Scan applies the selector query and scans the result into the given value.
func (fs *ForkSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fs.ctx, ent.OpQuerySelect)
	if err := fs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ForkQuery, *ForkSelect](ctx, fs.ForkQuery, fs, fs.inters, v)
}

func (fs *ForkSelect) sqlScan(ctx context.Context, root *ForkQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fs.fns))
	for _, fn := range fs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
